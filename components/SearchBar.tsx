"use client"
import Image from "next/image"

const SearchBar = () => {
    const handleSearch = () => {

    }
    return (
        <form className="searchbar" onSubmit={handleSearch}>
            <div className="searchbar__item">
                //implementing combo bars
            </div>
        </form>
    )
}

export default SearchBar